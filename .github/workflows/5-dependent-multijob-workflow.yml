name: Multi Workflow

on: 
  workflow_dispatch:

jobs:
  Multi-Workflow-1:
    runs-on: ubuntu-latest

    steps:
    - name: First step
      run: |
        echo "Hello From Mamun's Tutorial"
        

  Multi-Workflow-2:
    runs-on: ubuntu-latest
    needs: [Multi-Workflow-1]

    steps:
    - name: Second step
      run: |
        echo "Hello From Bobi's Tutorial"

  Multi-Workflow-3:
    runs-on: ubuntu-latest
    needs: [Multi-Workflow-1, Multi-Workflow-2]

    steps:
    - name: Third step
      run: |
        echo "Hello From Faiza's Tutorial"
        
    